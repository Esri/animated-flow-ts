// define([], () => {
//   return {
//     // this function can be invoked from the main thread
//     getMaxNumber: function (number) {
//       return Math.max.apply(null, numbers);
//     }
//   };
// });

// console.log("CIAO BAU!!");

export default {
  getMaxNumber() {
    return { data: 64 };
  }
};